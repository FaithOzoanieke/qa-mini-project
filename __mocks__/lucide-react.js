const React = require('react');

// Each icon is mocked as a simple <svg> element.
module.exports = {
  Eye: (props) => React.createElement('svg', props, null),
  Edit: (props) => React.createElement('svg', props, null),
  Trash: (props) => React.createElement('svg', props, null),
  Plus: (props) => React.createElement('svg', props, null),
};
